
How to run the application:
	1º) First you have to run the server:
		java -jar morse_server_application.jar
	2º) Once the server is running you have to run the clients:
		java -jar morse_client_application.jar
	3º) The messages from one clients to another are saved in a file 
	by the server, to read the messages you have to run the ReadLog application:
		java -jar read_log_application.jar
